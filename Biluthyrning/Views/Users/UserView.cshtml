@model Biluthyrning.Models.User

@{
    ViewData["Title"] = "UserView";
  
}

<h1>Användare</h1>

<h6>Välj i listan för vilken användare du är</h6>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Details">
            
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label hidden="hidden" asp-for="Id" class="control-label"></label>
                <select class="form-select" asp-for="Id" class="form-control" asp-items="ViewBag.Users">
                    <option value="none" selected disabled hidden>---Välj användare---</option>
                </select>
                <span asp-validation-for="Id" class="text-danger"></span>
            </div>
            <br />
            <div class="form-group">
                <input type="submit" value="Gå vidare" class="btn btn-primary"/>
            </div>
        </form>
    </div>
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}



